#!/usr/bin/env bash
# bin/release <build-dir>
 
cat << EOF
addons: []
default_process_types: {
web: ./bin/deploy --user system --password manager deploy --inPlace ./app_home 
web: ./bin/geronimo.sh run
}
EOF


