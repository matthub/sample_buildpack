#!/usr/bin/env bash
# bin/release <build-dir>
 
cat << EOF
addons: []
default_process_types: {
web: ./bin/geronimo.sh run
web: ./bin/deploy.sh --user system --password manager deploy --inPlace ./app_home 
}
EOF


