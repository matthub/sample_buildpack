#!/usr/bin/env bash
# bin/release <build-dir>
 
cat << EOF
addons: []
default_process_types: {
web: ./bin/geronimo.sh 
console: deploy --user system --password manager deploy --inPlace app_home
}
EOF
